当检测到可能的追索事件（StartEvent_1mnut37）后，流程进入检查案例任务（Task_0iirfhd）。检查完成后，流程到达排他网关（ExclusiveGateway_092mc05），根据“追索是否可能？”进行分支判断。如果追索可能，流程进入发送请求支付任务（Task_02fdytg），完成后进入事件基于网关（EventBasedGateway_0qdxz70）。在此网关处，根据不同的触发事件进行后续流程：如果收到分歧信（IntermediateCatchEvent_1ias0p2），则进入检查推理任务（Task_0eti3m2），判断后到达排他网关（ExclusiveGateway_0lk2nir），若判断为“OK”，进入关闭案例任务（Task_1qlbv5i）并结束流程；若判断为“否”，进入将案件移交催收机构任务（Task_1w7bb1w）并结束流程。如果未收到分歧信，而是提醒到期（IntermediateCatchEvent_037r6f2），则进入相应任务（Task_1w7bb1w）并结束流程。如果收到收款消息（IntermediateCatchEvent_0d430z1），则进入预订任务（Task_1bwmf45），完成后进入关闭案例任务（Task_0yan60f）并结束流程。如果追索不可能，流程直接进入关闭案例任务（Task_04aofbe）并结束流程。整个流程由资源办事员（Recourse Clerk）参与执行。