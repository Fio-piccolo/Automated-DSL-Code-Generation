以下是根据你的描述绘制的BPMN图的详细信息：

1. **BPMN图包含的元素及关系**：
   - 图中包含多个元素，如矩形框表示活动节点，菱形框表示网关，箭头表示流程线，还有一些文本标签用于标注节点名称等。
   - 例如，“Start”节点（起始节点）到“Activity1”节点之间有箭头连接，表明流程从此开始。
   - 还有判断节点（菱形）用于控制流程走向，如“Condition1”等。

2. **各个节点的标注信息**：
   - 如“End”节点（结束节点）等，每个节点都有对应的文本标注，如“Some Node"等，描述了节点的功能或阶段名称。

3. **连线和条件判断**：
   - 连线表示流程的流向，连线两端的节点之间通过箭头连接，并且在部分节点处有条件判断条件，如判断是否执行特定的流程路径。

4. **图形布局**：
   - 图中元素按流程顺序和逻辑关系分布，箭头指向正确的方向表示流程的前进方向。

通过这些信息，能够清晰地展示BPMN图中各个元素的位置、连线关系以及节点的标注内容，从而完整呈现BPMN的结构和流程逻辑。