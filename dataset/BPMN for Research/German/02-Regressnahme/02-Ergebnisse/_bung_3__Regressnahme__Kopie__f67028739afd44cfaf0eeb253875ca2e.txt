业务需求如下：当“REGRESSMÖGLICHKEIT WIRD GEMELDET”事件触发后，进入“PRÜFEN rEGRESSS”任务进行检查。检查完成后，通过“REGRESS MÖGLICH”排他网关进行判断，如果结果为“JA”，则执行“ZAHLUNGSAUFFORDERUNG VERSENDEN”任务并发送付款要求；如果结果为其他情况，则直接进入“kEIN rGERESS”结束事件。在发送付款要求后，通过事件驱动的排他网关进行分支判断。若“gELD IST EINGEGANGEN”事件发生，即资金已到账，则执行“eINGANG VERBUCHEN”任务进行入账，之后进入“REGRESS BEZAHLT”结束事件；若“wIDERSPRUCH EINGEGANGEN”事件发生，即收到异议，则执行“WIDERESPRUCH PRÜFEN”任务进行异议检查，检查后再次通过“GRRECHTFERTIGT”排他网关进行判断。若结果为“JA”，则进入“AN INKASSO WEITERGEBEN”任务并转交给催款部门，之后进入一个未命名的结束事件；若结果为“NEIN”，则执行“INKASSO WEITERGEBN”任务并继续进行收款相关操作，之后进入另一个未命名的结束事件。另外，如果“FRIST ABGELAUFEN”事件发生，即期限已过，则执行“INKASSO WEITERGEBN”任务并继续进行收款相关操作，之后进入又一个未命名的结束事件。