业务需求如下：当“REGRESSMÖGLICHKEIT WIRD GEMELDET”事件触发后，进入“PRÜFEN rEGRESSS”任务进行检查。检查完成后，通过“REGRESS MÖGLICH”排他网关进行判断，如果条件为“JA”，则执行“ZAHLUNGSAUFFORDERUNG VERSENDEN”任务并发送支付请求，之后进入“gELD IST EINGEGANGEN”中间捕获事件等待资金到账。资金到账后，执行“eINGANG VERBUCHEN”任务进行入账，完成后流程结束。如果在“REGRESS MÖGLICH”排他网关判断条件为其他情况（未明确说明），则进入“kEIN rGERESS”结束事件，流程结束。另外，如果在发送支付请求后，进入“eventBasedGateway”排他网关，若触发“wIDERSPRUCH EINGEGANGEN”中间捕获事件，即收到反对意见，则执行“WIDERESPRUCH PRÜFEN”任务进行审核。审核后，通过“GRRECHTFERTIGT”排他网关进行判断，如果条件为“JA”，则进入“AN INKASSO WEITERGEBEN”任务并转交给催款部门，之后进入结束事件流程结束。如果在“GRRECHTFERTIGT”排他网关判断条件为“NEIN”，则执行“INKASSO WEITERGEBN”任务并继续进行收款流程，最后进入相应的结束事件，流程结束。此外，如果在发送支付请求后触发“FRIST ABGELAUFEN”中间捕获事件，即期限到期，则执行“INKASSO WEITERGEBN”任务并继续进行收款流程，最后进入相应的结束事件，流程结束。