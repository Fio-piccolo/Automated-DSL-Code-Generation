业务需求如下：当收到“Regress gemeldet”（退回已报告）的信息后，启动流程。首先检查相关情况，若退回可行，则发送支付请求；若退回不可行，则直接关闭流程。在检查情况过程中，对相关案例进行核实，核实后根据退回可行性结果进行下一步操作。若退回可行，先将流程状态设置为“Wiedervorlage”（重新提交），再发送支付请求，之后将流程关闭；若退回不可行，则直接关闭流程。在流程执行过程中，可能会收到“Widerspruch VN”（来自VN的异议）或“Zahlungseingang VN”（来自VN的付款到账）等事件。收到“Widerspruch VN”后，对异议进行检查；收到“Zahlungseingang VN”且满足一定条件时，记录收款并继续流程。对异议检查后，若判定支付拒绝合理，分情况处理，若有其他相关任务则执行该任务，之后将流程关闭；若没有其他相关任务，则直接将流程关闭。流程还设置了定时任务，在经过特定天数后执行后续操作，执行完后续操作后最终将流程关闭。