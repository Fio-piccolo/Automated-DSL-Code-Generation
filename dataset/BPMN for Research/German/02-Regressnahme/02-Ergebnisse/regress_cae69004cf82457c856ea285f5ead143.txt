业务需求如下：当收到“Regressverdacht gemeldet”（退回嫌疑已报告）的消息后，启动流程。首先检查是否有可能退回，若有可能退回，则进入并行流程，一方面发送付款请求给VN，另一方面将流程恢复到待提交状态；若不可能退回，则直接结束流程。当收到“Geld eingegangen”（钱已收到）的消息时，记录收款；当收到“Widerspruch eingelegt”（提出异议）的消息时，检查VN的异议；当“Widerspruchsfrist abgelaufen”（异议期限已过）时，根据是否有退回可能性以及付款拒绝是否合理，进行不同操作。若有退回可能且付款拒绝合理，则结束流程；若有退回可能但付款拒绝不合理，则将流程交给收账服务提供商；若没有退回可能，则结束流程。最后，当流程到达“Vorgang geschlossen”（流程已关闭）或“Vorgang an Inkassodienstleister abgegeben”（流程已交给收账服务提供商）时，相应地结束流程。